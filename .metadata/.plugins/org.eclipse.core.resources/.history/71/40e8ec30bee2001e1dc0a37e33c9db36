package com.application.productsale.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.application.productsale.model.Role;
import com.application.productsale.model.User;

public interface UserRespository extends JpaRepository<Role, Integer> {
	
	User findByUsername(String username);

}
